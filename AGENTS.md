# Repository Instructions

- Before every commit, bump the package's patch version in both `package.json` and `package-lock.json` (for example, with `npm version patch --no-git-tag-version`). Every commit must have a newer patch version than the commit before it.
